# Editorial policy (your AI step)

This is where your editorial judgment lives — the part performed by Claude on the
Max subscription during the daily Cowork run. Replace this placeholder with your
real policy / prompt (e.g. your existing `daily-brief.md`).

The deterministic code in `src/` does NOT make editorial choices; it only fetches,
scores, de-duplicates, validates, gates and renders. Curation guidance goes here.

Suggested contents:
- Topics / beats to prioritize and to avoid.
- Source quality rules (primary vs secondary; trusted publishers).
- Recommendation portfolio rules (e.g. max per provider).
- Tone, length, and language of any written summaries.
- Anything that requires judgment rather than a deterministic rule.
